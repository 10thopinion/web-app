import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/theme-provider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Tenth Opinion Protocol - AI Medical Diagnosis",
  description: "Revolutionary multi-agent AI system for accurate medical diagnosis using AWS Bedrock",
  keywords: "medical AI, diagnosis, healthcare, AWS Bedrock, multi-agent system",
  authors: [{ name: "Tenth Opinion Team" }],
  openGraph: {
    title: "Tenth Opinion Protocol",
    description: "Democratizing access to expert medical opinions through AI",
    type: "website",
  },
  viewport: "width=device-width, initial-scale=1, maximum-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}